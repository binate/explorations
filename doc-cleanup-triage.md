# explorations/ doc-cleanup triage

> Working doc (2026-06-04). Classification of all 109 tracked docs by residual
> value, to drive a delete/slim pass. Git preserves history, so deletion loses
> nothing retrievable. **Delete it once the cleanup it describes is executed.**

Category = residual value today: **1** active · **2** info-of-interest ·
**3** some-info-but-mostly-spent · **4** historical-only.  
Anchors: **[C]** = listed in CLAUDE.md key-reference files; **[tN]** = referenced
N× by claude-todo.md (i.e. still wired into active work).

**Counts:** delete 11 · merge-historical 20 · slim 40 · keep 38  (109 total)

## DELETE — spent; substance is in code / git / the notes files / done-list — 11

- **claude-plan-charbuf.md** (cat3) — _status:_ STATUS (2026-03-31): COMPLETED — Plan for CharBuf, which shipped as buf.CharBuf and is used throughout; the design (managed-slice backing, geometric growth, return-by-value) is now embodied in the code and the CharBuf decision is recorded in claude-notes.md. Spent execution detail with no unique surviving rationale.
- **claude-plan-fix-make.md** (cat3) — _status:_ STATUS (2026-04-01): COMPLETED — Migration plan for make/make_slice semantics; all of it shipped (make_slice exists, make_raw_deprecated and append removed). The durable decision (make(T)->@T uniformity, make_slice for @[]T) is captured in claude-notes.md; the rest is a step-by-step commit checklist now in git history.
- **claude-plan-remove-append.md** (cat3) — _status:_ STATUS (2026-03-31): COMPLETED — Plan to remove append; fully shipped. The decision and its rationale (append is an O(n^2) footgun, growable collections are a library concern) are explicitly recorded in claude-notes.md; the doc's body is a per-file/per-step removal checklist now in git history.
- **claude-plan-runtime-package.md** (cat3) — _status:_ STATUS (2026-03-31): COMPLETED — Plan to move the C runtime into pkg/rt; fully shipped (Alloc/RefInc/RefDec/MakeManagedSlice/Box/BoundsCheck in Binate, thin C stubs only). The architecture is now the code itself; remaining content is migration steps and an inline draft of rt.bn, both superseded by the actual source.
- **claude-plan-slice-compiler.md** (cat3) — _status:_ STATUS (2026-03-31): COMPLETED — Plan to convert the self-hosted compiler off append/raw-slice misuse; completed. The append-call inventory and conversion order are spent; the design pieces (CharBuf, managed vs raw slice usage) live in claude-notes.md and the coding guide.
- **ir-backend-cleanup-plan.md** (cat3 [C]) — _status:_ Phase 2 marked DONE/IMPLEMENTED inline; rest unmarked — Work plan for multi-backend support that has fully shipped: TargetInfo/--target, layout extraction, slice-op inlining, runtime abstraction, and an ARM backend all landed (native aa64/x64 + arm32 lanes pass conformance). The durable 'why' lives in ir-backend-guidelines.md; this is spent execution detail. CLAUDE.md still links it, so update that pointer when removing.
- **layout-extraction-plan.md** (cat3 [C][t1]) — Detailed step-by-step plan for extracting layout into pkg/types; the work shipped (StructLayout, FieldLayout, TargetInfo, SetTarget all present in pkg/binate/types and used by codegen). The design rationale survives in ir-backend-guidelines.md; this is spent execution detail. CLAUDE.md links it, so update that pointer when removing.
- **plan-copy-constructors.md** (cat3) — _status:_ none (shipped — gen_copy.bn/gen_copy_emit.bn exist; conformance 222-225 in done-list) — Work shipped (copy-function infra mirroring dtors is in pkg/binate/ir; the done-list documents the principled slow-path that actually landed, which diverged from this plan's exploratory Option-A/B musings). The why now lives in design-refcount-axioms.md + bug-struct-copy-refcount.md + the done-list; this doc is spent execution detail and stream-of-consciousness ordering analysis.
- **notes-profiling-bootstrap-2026-05-01.md** (cat4) — _status:_ none (carries explicit 'Don't invest' / shelf-life caveat) — Profiles the Go bootstrap interpreter, which was retired 2026-05-21; the note itself recommends not investing because the bootstrap is throwaway, so its findings (newEnv allocation / GC pressure) are moot for code that no longer exists.
- **plan-4word-managed-slice-destructors.md** (cat4) — Both the 4-word layout and destructors are IMPLEMENTED and finalized per claude-todo-done.md, and the full ratified design (layout, subslicing-from-refptr, dtor dispatch) now lives in claude-notes.md. This early design doc adds nothing beyond what notes + code + git capture.
- **plan-4word-migration.md** (cat4) — _status:_ none (explicitly 'destructors are a follow-up') — Pure spent execution checklist (file:line edit list) for a migration that finalized; references retired pkg/interp and bootstrap interpreter. The decision/rationale lives in claude-notes.md; nothing here is the sole record of anything.

## MERGE-HISTORICAL — delete after a one-line pointer (commit / old file) in a new historical-notes.md — 20

- **claude-plan-managed-headers.md** (cat3) — _status:_ STATUS (2026-03-31): COMPLETED — Completed 3-word managed-slice/@T-header design, since superseded by the 4-word layout in plan-4word-migration.md. The one durable nugget is the prefix-layout rationale (@[]T's first two words match *[]T, so it reads as *[]T with no arithmetic); claude-notes.md covers layout but a one-line pointer to this design is worth keeping before deletion.
- **plan-const-nonint.md** (cat3 [t1]) — _status:_ none (superseded by plan-const-readonly per its own text + claude-todo.md) — Superseded: plan-const-readonly restructured the const/var/readonly surface; Phase A survives there scalar-only, Phase B (composites) was explicitly CANCELED, Phase C became var readonly *T. The only durable nugget is its lucid statement of Binate const semantics (immutable variable, not reducible-to-immediate), which is already captured in claude-notes.md; worth a one-line historical pointer before deletion.
- **plan-ir-gen-typed-literals.md** (cat3) — _status:_ Planned 2026-05-22, landed 2026-05-23 — Fully shipped (A1-A5, B all on main; corroborated in claude-todo-done.md) and the magnitude-heuristic bug it fixed is gone from the tree. Mostly spent execution/commit-log detail now living in git + the done-list; the one durable nugget is the bignum->IntVal conversion rules (two's-complement tricks for int64-min and uint64-only positives), worth a one-line pointer before deletion.
- **plan-self-type.md** (cat3) — _status:_ COMPLETE 2026-05-13 (all four slices landed) — Fully shipped, and the doc itself points to claude-notes.md as the canonical spec and claude-todo-done.md as the ratification record — so the "why" already lives elsewhere. The only residual nuggets are the implementation-notes (TYP_SELF singleton identity, substitution-under-aliases) and the object-safety rejection rationale, both of which are minor; leave a one-line pointer to commits dca093f/b780acd/77369e7/4c33df7 before deletion.
- **plan-string-literals.md** (cat3) — Shipped: TYP_STRING/VAL_STRING/bn_string_to_chars/OP_STRING_TO_CHARS are all gone, string literals are now static @[]const-char globals. The why (null backing_refptr => immortal via no-op RefInc/RefDec on null) is the one nugget worth a one-line historical pointer; the rest is spent and duplicated by the done-list.
- **plan-struct-temp-cleanup.md** (cat3) — _status:_ none (4 failed attempts documented; later resolved elsewhere) — Test 226 now passes (no xfail file) via the principled slow-path recorded in design-refcount-axioms.md; this doc is a writeup of four failed statement-level attempts before that fix. The durable nugget (the struct-return-move-skips-dtor-leaves-extra-field-RefInc root cause) already lives in the axioms doc, so a one-line pointer suffices before deletion.
- **plan-vm-64bit-on-32bit.md** (cat3 [t1]) — _status:_ COMPLETE / end-to-end-validated on arm32 (2026-05-28) — All steps landed and validated; the status header itself points to claude-todo.md for the commit trail, so the execution detail is spent. The one durable nugget is the 'register size == host word size, pay-for-64-only-when-64-bit' decision plus the single-post-pass-with-BC_RETURN-exception rationale — worth a one-line historical pointer before deletion.
- **plan-vm-compiled-closure-dtor.md** (cat3 [t1]) — _status:_ DONE 2026-06-03 (binate 0a0d00af) — Fully shipped; the leak fix and conformance test (550_func_value_capture_released) are now in the code. The residual nugget is the sentinel-design rationale (why a dedicated dtor-handle sentinel rather than reading rec[0]) — worth a one-line pointer to the commit before deleting.
- **slice-operations-analysis.md** (cat3) — _status:_ none (table marks all analyzed ops 'removed' or 'remaining') — Phase 2 analysis whose conclusions all shipped — slice ops lowered to IR primitives, append removed, raw-slice subslice copy bug fixed. Durable nugget worth a one-line pointer: the 'Decision: Lower Slice Ops in IR Gen' rationale (slice layout is a language-level contract, so lowering happens once in IR gen, not per backend), though that already lives in runtime-abstraction-plan.md's step 3.1.
- **bug-struct-copy-refcount.md** (cat4) — _status:_ Status: FIXED in both compiler and interpreter — Bug is fixed (fix commits cited in the doc; referenced from claude-todo-done.md) and covered by copy-constructor generation now in the tree, so little reason to read today. Worth a one-line historical pointer naming the nugget: missing RefInc on by-value struct copies of structs with managed @[]T/@T fields, fixed by __copy_X generation (commit 2052570) + interpreter structRefInc (78f959c).
- **claude-bootstrap-plan.md** (cat4) — _status:_ Header NOTE flags append as outdated; per-step DONE markers; Step 10 'TODO' — Describes the implementation of the Go bootstrap interpreter, which has been retired entirely; the only non-obvious nugget is the bootstrap-subset boundary and pkg/bootstrap I/O surface, both of which are also captured elsewhere (grammar.ebnf [BOOTSTRAP], claude-plan-1/2). Leave a one-line historical pointer, then delete.
- **compiler-loop-var-stack-leak.md** (cat4) — Per-iteration alloca leak for loop-body vars; conformance/268_compiler_loop_var_leak.bn now exists with no xfail markers, so the bug is fixed and the test guards regressions. Little residual value; keep a one-line pointer noting the root cause (in-loop OP_ALLOC not hoisted to entry block) and that test 268 covers it, then delete.
- **native-aa64-bugs.md** (cat4) — _status:_ All three clusters RESOLVED; final state 29/29 unit + 285/285 conformance — Point-in-time postmortem of the native aa64 unit-test sweep; every bug (imm12 STR truncation, ARM64_RELOC_PAGE21 r_extern, sub-word multi-return, cross-pkg by-value struct ABI) is fixed and the fixes live in code/tests with commit hashes. Little reason to read today; worth a one-line historical pointer naming the imm12-truncation root cause and the regPool-saturation follow-up before deleting.
- **notes-profiling-bnc-2026-04-29.md** (cat4) — _status:_ Status (2026-04-29, later same day): Recommendations #1 and the -O2 link bug addressed and re-profiled; preserved as original baseline — Point-in-time -O0 baseline whose recommendations all landed (addStructDef remangling fix, weak_odr dtor fix) and were re-profiled in two follow-ups; the one durable nugget is the addStructDef O(M*N) remangling defect and its fix commit c884838, worth a one-line historical pointer before deletion.
- **notes-profiling-bnc-followup-2026-04-29.md** (cat4) — _status:_ Status (2026-05-01): re-profiled after OP_REFDEC inline lowering landed; preserved as v3 baseline after addStructDef and weak_odr fixes — v3 -O2 snapshot, itself superseded by the v4 follow-up; the durable nugget is the linkonce_odr->weak_odr dtor-discard root cause (commit 65cb258) plus the f08ddcb possible-regression observation, worth a one-line pointer before deletion.
- **notes-profiling-bnc-followup-2026-05-01.md** (cat4) — _status:_ none (latest in the bnc chain) — Final v4 bnc self-compile profile showing RefDec/RefInc inlined away and clang dominating ~80% wall; conclusions (diminishing returns, incremental-compilation is the real lever) are general and the numbers are stale. One live nugget: the open '-g emits invalid LLVM IR after OP_REFDEC inline' bug (addDbgToLastLine on label lines) — confirm it's tracked in claude-todo before deleting, else keep the pointer.
- **notes-profiling-bni-2026-04-30.md** (cat4) — _status:_ Status (2026-05-01): OP_REFDEC inline lowering landed; preserved as original baseline + CallCache result — Baseline bni fib(36) profile whose headline fix (per-VMFunc CallCache replacing per-call linear LookupFunc scan, commit 6c8e0c0, -30% wall) has shipped and is captured in code; worth a one-line historical pointer to the CallCache rationale (invalidation-under-REPL design) before deletion.
- **notes-profiling-bni-followup-2026-05-01.md** (cat4) — _status:_ none (latest in the bni chain) — Final bni profile after OP_REFDEC inline (cumulative -43% wall from v1); work has landed and remaining levers (dispatch-loop threading, BoundsCheck elision) are generic future-direction notes also stated elsewhere. Low residual value beyond a pointer to the full v1->v3 arc.
- **plan-codegen-byval-spill-hoist.md** (cat4) — _status:_ LANDED 2026-06-02 (440485b0 + d9800429); MAJOR (resolved) — Bug (call-site byval-spill alloca leaking per loop iteration) is fixed and verified; the durable rule (allocas belong in the entry block, LLVM idiom) is generic LLVM lore, not Binate-specific. Worth a one-line historical pointer to commits 440485b0/d9800429 naming the entry-block-hoist fix before deleting.
- **plan-managed-fields.md** (cat4) — _status:_ none (one section marked REMOVED 2026-04-17) — The migration shipped — ast.bni now has the exact converted fields (Name @[]char, Args @[]@Expr, Elems @[]@Element) the plan enumerated, and the rationale ('raw slice fields dangle when @T is freed; every managed-struct field with separate backing must itself be managed') already lives in the coding guide. Per-field checklist and the now-deleted pkg/interp section are spent; leave a one-line pointer to the rationale and bottom-up migration approach.

## SLIM — keep, but cut spent execution detail / stale status (the *why* stays, the step-by-step goes) — 40

- **binate-coding-guide.md** (cat1 [C]) — Living doc explicitly cited by CLAUDE.md and read before writing Binate code; actively normative. Slim only the stale scoping note: the top banner and several sections frame everything around the retired 'bootstrap subset / Go bootstrap interpreter', which should be reframed to the current BUILDER-compatibility constraint.
- **runtime-abstraction-plan.md** (cat1) — _status:_ Phase 3: steps 3.1-3.3 DONE, 3.4 PARTIAL, 3.5-3.7 not started — In-flight plan: file I/O, entry-point rework, and syscall abstraction (3.4-3.7) are still pending and this is their design reference for the libc-free target. Slim the spent DONE step writeups (3.1-3.3, with commit chains already duplicated in claude-todo.md) while keeping the architecture and remaining-work design (3.4-3.7).
- **README.md** (cat2) — _status:_ Status: Early design and exploration phase — Public-facing project overview (goals, targets, dual-mode pitch) is durable reference, but the 'Status: Early design and exploration phase' line is badly stale given the self-hosted toolchain is implemented and stable; trim/update the status section.
- **ownership-model-managed-params.md** (cat2) — _status:_ none (contains an open investigation and an Open Question) — Sole prose record of the @T parameter ownership model (callee-side RefInc, transfer-vs-RefInc invariant) and the still-unresolved move-semantics-as-guarantee design question — durable rationale not captured in code. Slim the stale interpreter-specific sections: the bootstrap interpreter (envDefine-side RefInc) is retired, and the 'Discrepancy: boot-comp-int tests 228/229 rc=3' investigation is point-in-time and likely resolved; verify and trim those.
- **plan-c-call.md** (cat2 [t2]) — _status:_ all 4 stages DONE 2026-05-27..2026-06-02 — The __c_call feature has fully shipped, but the pkg/c C-type alias table (C_int=int32 not Binate int on LP64, the C_void/varargs marker recognition-by-qualified-name decision) and the bootstrap-adoption-waits-for-BUILDER-bump cadence are durable rationale worth keeping. Slim the now-spent §6-7 backend/staging execution detail (each stage's landed-commit prose) down to pointers.
- **plan-const-type-modifier.md** (cat2) — _status:_ Status: Planning only. No code written. (stale — TYP_CONST machinery shipped, then renamed to readonly) — The const-type-modifier shipped (TYP_CONST in pkg/binate/types, later renamed readonly), but this doc is the sole detailed record of the assignability lattice (T <= const T widening table), the literal-init copy rule, and the per-encounter composite-literal allocation + UB-on-shared-rodata decision — durable design rationale not in code. Slim the duplicated 'String literals' section (appears twice verbatim) and the stale 'Planning only' status; keep the semantics/assignability/UB content.
- **plan-float-arg-shim.md** (cat2 [t2]) — _status:_ MECHANISM LANDED on main (7abc3809), 2026-06-03 — Design A — Sole record of a non-obvious decision: the uniform all-int shim ABI (Design A) and why the float-aware-VM alternative (Design B) was rejected, plus the latent -int miscompile it fixes. Slim the spent execution detail (per-step landing checklist, verification matrix, file:line affected-sites) now that it has landed; keep the Problem / Design A / Design B sections.
- **plan-floats.md** (cat2) — _status:_ Planning only. No code written yet. — Status says 'no code written' but floats have clearly shipped (float-arg-shim plan, conformance/287_float_println). It is the sole detailed record of float design cuts (no implicit int<->float, untyped-float defaults to float64, %/bitwise rejected, NaN/Inf syntax punted); claude-notes.md only has the high-level 'Primitive types — DECIDED'. Slim the now-spent per-commit implementation breakdown and stale Status; keep Scope/in-out-of-scope and the type rules.
- **plan-function-values-phase-2.md** (cat2 [t1]) — _status:_ LANDED (2026-05-31; cleanup miscompile fixed 2026-06-01) — Shipped, but it is the sole consolidated record of the closure/method-value capture design (capture-by-value, no capture lists, the 7-shape receiver-smoothing table, vtable structural identity, the slot-0 dtor-pointer miscompile). Slim the per-slice B.1..B.6 landing log (now in git/done-list) while keeping the Decided-design sections.
- **plan-function-values.md** (cat2 [C][t4]) — _status:_ Phase 1 COMPLETE (2026-05-01) — The umbrella plan and richest record of the function-value representation rationale (2-word {vtable,data}, dtor-first vtable layout, why function values are the interop substrate, the VM-mode option-1/2/3 trade study). Note it still records check-data-nil as DECIDED while phase-3 reversed it to always-shim — slim/reconcile that, and trim the Phase-1 per-slice A.1–A.7 progress log.
- **plan-generics.md** (cat2) — _status:_ COMPLETE 2026-05-21 — Generics shipped, but this is the sole record of the non-duck-typing constraint-satisfaction model, the primitives-implement-interfaces dependency, monomorphization-to-direct-calls, and the Q7 '.bni source-text bodies' decision and rejected alternatives. Slim the Slice 1–7 commit-by-commit landing log; keep Ratified decisions and the still-informative Q1–Q8.
- **plan-interface-embedding.md** (cat2) — _status:_ DONE 2026-05-13 — Shipped; the design summary is partly mirrored in claude-notes.md (flipped to 'implemented'). Durable nuggets: the concat any-block][parent-vtables][own-methods vtable layout, static-offset upcast, transitive-impl registration, and the method-conflict/cycle edge-case decisions. Slim the per-slice E.1–E.5 sizing/file-list execution detail; keep the ratified design summary and open-question resolutions.
- **plan-repl.md** (cat2 [t8]) — _status:_ Tier 1-4 LANDED; Tier 5 LANDED; small Tier 4 follow-ups + pretty-printer remain (gated on interfaces) — All five tiers functionally shipped, so most of the long per-tier "what shipped" changelog is spent. But it remains the consolidated record of REPL design rationale (interpreter-only-first reasoning, replace-vs-shadow semantics, CallCache-freeze rationale, deferred-pretty-printer gating) and a few real open items (refcount-aware shadow warning, forced-shadow hatch, pretty-printer). Slim the redundant per-tier transcript tables; keep scope/rationale, decided-semantics, and the open follow-ups.
- **plan-std-errors.md** (cat2 [t6]) — _status:_ none (in-doc: "Settled through discussion 2026-06-02/03") — The errors package landed (conformance 545/577), so the Part 0/Part 1 execution detail is spent — but this is the SOLE record of the non-obvious present()/empty-not-nil decision and its rationale (typed-nil dishonesty, mirror of rt.HasBacking), plus still-open deferrals (Is/As, MultiError, Errorf). Slim the implementation walkthrough; keep the design-rationale and Deferred sections.
- **plan-version-info.md** (cat2 [t1]) — _status:_ none (Phase 2/3 marked 'deferred') — Phase 1 (version package + gen-version.sh) has shipped — files exist, VERSION=bnc-0.0.7-pre — and the active --version-wiring follow-up now lives in claude-todo.md; that spent Phase-1 execution detail should be trimmed. Phases 2 (buildinfo) and 3 (const-in-.bni semantics) are unbuilt and hold non-obvious open design decisions worth keeping.
- **refcount-lifecycle.md** (cat2) — _status:_ none (status section dated 2026-04-12) — Durable normative spec of the refcount ownership model and per-context rules (the 'why', not in code) — high value. But the 'Current Implementation Status' (stale '187/187 on boot-comp'), interpreter sections (the interpreter was removed per memory), and the dated changelog are stale point-in-time noise that should be trimmed, leaving the normative spec.
- **claude-plan-1.md** (cat3) — _status:_ Phase 1-4 'complete'; per-item DONE markers throughout — Phases 1-4 are fully shipped and the Go bootstrap they target is retired; the syntax/semantics decisions it lists are duplicated (with rationale) in the two notes files and grammar.ebnf. Slim by dropping the spent Phase 4 implementation-status detail; its only mild residual value is the consolidated Phase 1-2 decision checklist.
- **claude-plan-2.md** (cat3 [C]) — _status:_ Status: 'Phase 5a complete. Phase 5b (compiler) in progress — see claude-plan-3.md.' — Self-hosted toolchain is shipped and stable, so the execution plan is spent. A few durable nuggets remain (interpreter-first rationale, future repo-split sketch, backend/object-file/inline-asm direction); slim by cutting the per-package DONE status table and superseded Go-bootstrap framing.
- **claude-plan-3.md** (cat3) — _status:_ Status: 'Steps 1-15 complete. Step 14 done (self-compilation achieved).' — Nearly all steps marked DONE and self-compilation achieved, so the bulk is spent execution detail tied to the original LLVM-text/C-runtime backend that has since evolved. Slim hard, or merge-historical; durable design notes (string representation, layout rules, ownership) already live in claude-notes.md / claude-discussion-detailed-notes.md.
- **claude-plan-4.md** (cat3) — _status:_ none (per-feature 'No' in Current Status table, all pre-implementation) — Most listed features (methods, interfaces, closures, refcounting, generics) have since shipped with their own dedicated plan-*.md docs and claude-notes.md sections, so this high-level roadmap is largely overtaken; the 'No/No/No' status table is stale. Residual value is only the few still-unbuilt items (REPL, annotations, floats); slim to those or merge-historical.
- **design-bytecode-interpreter.md** (cat3) — _status:_ Status note (2026-04-17): historical design rationale — The bytecode VM (pkg/vm) shipped and is the sole interpreter, so the implementation-plan phases and the pkg/interp-comparison framing are spent. The durable nugget is the core design rationale (inherit IR-level refcounting for correctness) and the IR-op to bytecode-op mapping; slim away the retired-pkg/interp comparison and the Phase 1-5 plan.
- **pkg-layout-plan.md** (cat3) — _status:_ All steps LANDED, captured by the bnc-0.0.5 release — All eight steps shipped (bnc-0.0.5); the bulk is spent per-step execution detail with commit hashes that git already records. Worth slimming to retain the two durable nuggets the spec lacks: the BUILDER-skew/gen1-routing trap (Pre-Step-4) and the premature-release-cut guardrail, both of which are non-obvious 'why' lessons for future migrations.
- **plan-backend-objformat-decoupling.md** (cat3) — _status:_ DRAFT (2026-05-26); 4 open questions resolved — The core work shipped (nativeObjFormatForTarget in cmd/bnc/target.bn; the native_x64_darwin/Rosetta conformance runner exists), so the phasing detail is mostly spent. Worth slimming rather than deleting: the four Resolved-decisions (esp. that symPrefix is the ONLY format-divergent naming knob, applied at three site categories) and the axis-coverage CI rule are non-obvious design conclusions not captured in code or claude-notes.
- **plan-bnc-as-builder.md** (cat3) — _status:_ none (phased plan, fully executed) — Fully executed: bootstrap interpreter retired, BUILDER_VERSION=bnc-0.0.6, builder modes canonical. Most phases are spent. Worth slimming to a short record rather than deleting because it is the sole design rationale for the BUILDER_VERSION/VERSION convention, the chicken-and-egg release loop, and the rebuild-from-source escape-hatch base case — the 'why' behind the current build infrastructure.
- **plan-call-indirect.md** (cat3 [t1]) — _status:_ LANDED — OP_CALL_INDIRECT landed and rt.CallDtor is retired; the bulk is spent implementation scope/phasing. The resolved open-questions header (signature-from-instr.Typ, no user-facing builtin, the cross-mode fn_ptr convention: real pointer compiled / 1-based VM index) is the non-obvious why worth retaining — slim the long historical body below it.
- **plan-codegen-byval.md** (cat3) — _status:_ LANDED 2026-05-30 (f5340fac + 8ba29d11) — Feature shipped, but the implementation diverged from the plan in a load-bearing, non-obvious way recorded only here: emit plain `ptr` (not `ptr byval(T)`) because LLVM AArch64's byval lowering lays the struct on the caller stack rather than the indirect-pointer form. Keep that divergence note + root-cause; slim the spent sites-to-touch/phasing/native-aa64 execution detail.
- **plan-codegen-c-free-copies.md** (cat3) — _status:_ LANDED 2026-06-01 (all 8 steps + OP_LOAD followup) — The memcpy-free-codegen work shipped with a per-step landing-commit table; the bulk is spent survey/execution detail. Keep the C-free-target rationale and the durable per-field-vs-aggregate caveats (padding-bytes undefined, atomic-store ordering lost, native↔LLVM ABI byte-image agreement); slim the per-site survey and sequencing.
- **plan-composite-literal-generalization.md** (cat3) — _status:_ none in-doc; Phases 1/2/3 all DONE per claude-todo-done.md (2266-2284, 1784, 1455-1475) — All three phases shipped, so most of the per-phase parser/checker/IR/codegen execution detail is spent. The const-ness-of-element design table (why *[]T{...} is rejected but *[]const T{...} allowed; rodata-vs-stack-backing lifetime reasoning) is non-obvious language-semantics rationale worth keeping; slim the rest. The doc lacks an in-file status marker — add one.
- **plan-cross-package-interfaces.md** (cat3) — _status:_ feature-complete; all four slices (2.6/2.7/2.8/2.9) DONE (2026-05-07) — Shipped feature; bulk is spent per-slice execution log. But it records non-obvious ratified decisions — canonical (R,I) vtable mangling, duplicate-OK-no-orphan-rule, weak_odr dedup — and a still-open deferred item (mixed-mode compiled/VM cross-package iface dispatch). Slim the DONE per-slice implementation narratives down to the ratified design points + the one open follow-up.
- **plan-function-values-phase-3.md** (cat3 [t4]) — _status:_ DRAFT (header) — but all slices 3.1–3.4 marked LANDED in body — All four slices landed despite the stale DRAFT header; mostly spent execution detail. Durable nuggets worth keeping: the always-shim vs check-data-nil departure rationale and the kind-tag data-record scheme. Slim the per-slice play-by-play and fix/drop the DRAFT marker; or merge-historical if the always-shim rationale is folded into the parent plan.
- **plan-linter.md** (cat3) — _status:_ none (Priority: Moderate) — The linter shipped — pkg/binate/lint + cmd/bnlint exist with managed-to-raw-assign/raw-slice-return/etc. rules implemented and tested. The shipped architecture/rule mechanics are now redundant with code; trim those. Keep the rule-rationale (which footgun each rule targets) and the still-open v2 items (suppression comments, data-flow precision, remaining unshipped rules) as the residual value.
- **plan-package-search-paths.md** (cat3 [t6]) — _status:_ Phase 1 complete (Stages 1-6); Stages 7-8 deferred — The shipped Phase 1 (two-path -I/-L loader split) is now spent execution detail, but the doc remains the sole design reference for deferred Stages 7 (env vars) and 8 (binary .o/.a/.so artifacts) plus the resolved CLI/naming decisions. Slim the landed-stage body, keep the motivation, deferred-stage design, and resolved decisions.
- **plan-pointers-to-iface-values.md** (cat3) — _status:_ Slices P.1-P.4 LANDED; P.5 DROPPED — All implementable slices landed (the feature shipped), so the bulk is spent; but each slice's LANDED note records the specific root cause and fix (e.g. P.2's TYP_MANAGED_PTR deref miss, P.4's typ-hint bypass) which is the sole written why. Slim the current-state probing section; keep the root-cause notes or migrate them to historical notes.
- **plan-print-builtin-runtime-decoupling.md** (cat3) — _status:_ Steps 1-3.1 DONE; Step 4 (bn_exit) deferred — The decoupling shipped (print/println now depend on the single bootstrap.Write sink; bn_print_* deleted), so the step plan is spent. Durable nuggets remain: the bootstrap-grade formatFloat semantics (NaN/Inf, fixed-point envelope, mantissa*2^exp fallback) and the deferred bn_exit cleanup question. The Mode Map's 'boot' Go-interpreter rows are stale (Go interp retired 2026-05-21) — slim those and the landed step detail.
- **plan-refcount-inlining.md** (cat3) — _status:_ DONE (RefInc + RefDec) — Work shipped (11 commits, confirmed in claude-todo-done.md "Inline RefInc / fast-path inline RefDec — DONE"). The bulk is spent execution detail, but the Option A vs B analysis — and especially the rationale that the VM win comes from one fused dispatch, not from inlining the sequence — is a non-obvious decision worth keeping. Slim the phasing/migration/open-questions sections, keep the Option-B-with-shared-helper rationale and the atomic-refcount future note.
- **plan-repl-tier3-pending-types.md** (cat3 [t2]) — _status:_ ALL STAGES LANDED (2026-05-29) — All 9 commits landed; the parent plan-repl.md already summarizes the outcome. The doc explicitly duplicates each stage as a LANDED summary PLUS the verbatim "original design notes ... for historical reference" — those original-notes blocks are the slim target. The resolved design decisions (sized-vs-reference per-caller check, group-as-syntactic-sugar, pending-method-is-pending) are the durable nuggets to retain.
- **plan-rt-ccall-drop-libc.md** (cat3) — _status:_ DONE (2026-06-03) — Approach A landed — Shipped (confirmed claude-todo-done.md "Drop pkg/libc via __c_call in rt — DONE"). The Approach A vs B trade-off and the user's ratified rationale for choosing native-only rt ("rt must be native" invariant, package-level native registration direction) is the one non-obvious decision worth keeping; the concrete-steps/verification-matrix is spent. Slim to the decision + rationale and the "this is NOT C-freedom" caveat; keep the rt.Exit-is-wrong-paradigm follow-up note.
- **plan-strconv-float.md** (cat3) — _status:_ COMPLETE (landed 2026-06-03) — All 7 steps landed and validated (208k-case Go differential). Most of the doc is spent execution detail now living in code, but it holds durable non-obvious design facts (uint32-limb overflow bound, ILP32 bit_cast width rule, Dragon4 unequal-gap subtlety, div-by-zero=panic rationale) and live follow-ups (signed Int storage-rework note, 'b'/'x' formats). Slim the step-by-step / test-matrix bulk; keep decisions + risks + 'Noted for later'.
- **plan-type-decls.md** (cat3) — _status:_ Phase 3a/3b/4/5 all LANDED 2026-05-29 — The whole single-source-of-truth series landed (forward-decl syntax, opaque-handle cross-pkg, mismatch=hard-error), so phases/sizing are spent. Keep the design model (the forward-decl visibility table + the Phase-3b gen_return.bn ptr-vs-struct root cause) as the sole detailed record of those non-obvious decisions; cut the per-phase landing logs already captured in the done-list.
- **plan-uniform-native-fnptrs.md** (cat3) — _status:_ Phases 1-4 LANDED (2026-05-24); migration steps all DONE — Fully landed across many commits with the running log baked in. Keep the durable architecture (function-value handles, why the shim drops data so trampolines need the raw function addr, the no-new-C constraint, the 1-based-idx audit table of what stays intra-vm) as design reference; cut the per-commit landing logs and superseded option-α/β/γ deliberations that no longer reflect the tree.

## KEEP — active, or durable high-value reference — 38

- **claude-notes.md** (cat1 [C][t11]) — _status:_ DECIDED markers throughout per section; doc-level: none — Living authoritative summary of language design decisions and rationale, explicitly named as a key reference in CLAUDE.md and cross-referenced by every active plan-*.md doc. Records the 'why' that code cannot capture; still updated (entries dated through 2026-06-01).
- **claude-todo-done.md** (cat1 [C]) — _status:_ none (living done-list; entries individually marked DONE/FIXED/RESOLVED) — Active living companion to claude-todo.md, updated as recently as 2026-06-03; the canonical record of resolved items (symptom/root-cause/fix/commit) that the cleanup itself treats as a deletion-safe destination. Keep as-is.
- **claude-todo.md** (cat1 [C]) — _status:_ none (per-entry markers like FIXED/IN PROGRESS/DEFERRED) — The authoritative living open-work-items list (3039 lines, entries dated through 2026-06-04, explicitly referenced by CLAUDE.md as the source of truth for active work and open bugs). Updated constantly; resolved items already migrate to claude-todo-done.md.
- **code-hygiene-check.md** (cat1) — Living reference describing the hygiene checks (file/line length, test coverage, .bni/.bn doc rules, conformance whitelist) that map to scripts/hygiene/; actively consulted before commits. Section 7 (bootstrap subset) is slightly stale post Go-interp retirement but defers correctly to bootstrap-subset.md rather than duplicating.
- **grammar.ebnf** (cat1 [C]) — The authoritative grammar specification (661 lines), referenced by CLAUDE.md as the canonical grammar. Living reference doc; the [BOOTSTRAP]/[DEFERRED] annotations are now historical (Go interp retired) but are interleaved with the only formal grammar, so the file stays as-is.
- **pkg-layout-spec.md** (cat1 [t6]) — _status:_ none (most structure aspirational; tiers 1/1x not yet authored) — This is the authoritative spec for the pkg/ tier organization and the ifaces/impls split; it supersedes a TODO entry and still governs unfinished work (tier 1/1x design, package-manager interaction). Living reference, not spent.
- **plan-macho-dysymtab.md** (cat1) — _status:_ design / not started; Severity: MAJOR — Genuinely un-started: macho.bn still emits ncmds=3 (SEGMENT+SYMTAB+BUILD_VERSION), LC_DYSYMTAB exists only as a const, no symtab sort. This is the complete actionable spec for an outstanding MAJOR ABI-compliance task (sort order, reloc r_symbolnum remap, field offset shifts, test list); keep until executed.
- **plan-native-globals.md** (cat1) — _status:_ Steps 1, 2, 5 done (checkboxes); Steps 3, 4 open — In-flight plan: the asm/Mach-O infrastructure (steps 1/2/5) has landed but the native-emitter work (steps 3, data section; 4, ADRP+ADD wiring) is still open, with a concrete list of conformance tests it unlocks. Living doc for unfinished work.
- **plan-native-x64.md** (cat1) — _status:_ Status: ratified 2026-05-22 — Ratified, detailed multi-phase plan for a native x86-64 backend; 'currently only consumer' / 'EmitObject routes only arm64' wording indicates the work has not started. Active forward-looking design doc with resolved decisions worth keeping intact.
- **plan-repl-embeddable.md** (cat1 [t4]) — _status:_ Stages 1-5 DONE; Stage 6 (suspend) + Stage 7 (break) FUTURE — In-flight living doc: v1 shipped but Stages 6-7 (continuable suspend, break) are explicitly future and gated on plan-bni-heap-frames.md, with the seam already built to receive them. The ratified-decisions table, the heap-frame/inline-cleanup invariants, and the open risks are the active spec for that remaining work.
- **plan-static-managed-sentinel.md** (cat1 [t3]) — _status:_ IN PROGRESS (worktree temp-binate-6 / branch work-6); steps 1-5 landed, emitter not yet called from non-test code — Active in-flight work (confirmed claude-todo.md "Static-managed sentinel refcount — IN PROGRESS"); the substrate landed but lacks runtime conformance coverage pending the first Phase B descriptor node. The sentinel-encoding decision (negative-as-immortal), the five-refcount-path table, and the open questions (header/payload adjacency, overflow aliasing, null-backing unification) are the live spec for finishing it.
- **plan-strconv-parse.md** (cat1) — _status:_ IN PROGRESS (2026-06-03) — Work is genuinely underway: num_error.bn and parse_bool.bn landed, but atoi.bn and parse_float.bn are not yet in the tree. This is an active living plan for the remaining ParseInt/ParseUint/ParseFloat work; claude-todo.md points at it as the plan-of-record.
- **plan-wasm-browser.md** (cat1 [t1]) — _status:_ none (Tier A/B0 detailed, B1-B4 named/deferred) — Wasm is not yet started (no wasm32 in bnc, no runtime files) and the plan is the live design of record, cross-referenced from claude-todo.md's wasm B1 entries. In-flight future work — keep as-is.
- **plan-x64-elf-e2e.md** (cat1) — Small, still-actionable plan: the x86-64 ELF e2e test file is not present in pkg/asm/elf (no elf_x64_test.bn / R_X86_64_PC32 markers found), so the work is unstarted. The doc is the concise spec for it.
- **release-process.md** (cat1 [t1]) — Living operational runbook for cutting bnc-X.Y.Z releases; references current infra (release.yml, BUILDER_VERSION, version-sync hygiene check) and is consulted/used every release. Sole written record of the release workflow and its non-obvious traps (BUILDER-skew, the two-VERSION-bump dance).
- **version-history.md** (cat1) — _status:_ current cycle bnc-0.0.5-pre, no headline feature yet — Authoritative, actively-updated record of the build ladder and each release's salient facts; sole record of non-obvious per-release decisions (e.g. why 0.0.2 stayed in the ladder despite the generics parser bug, the 'validate headline feature end-to-end before cutting' lesson). Updated each release cycle.
- **assembler-design.md** (cat2) — _status:_ Sections marked DECIDED / IMPLEMENTED throughout — This is the authoritative spec of the assembler's syntax, directives, data-emission naming, and library API conventions (the 'what we settled on' contract) that code alone does not capture; still worth consulting. The deferred/TODO list at the bottom is the only mildly stale part.
- **assembler-discussion.md** (cat2) — Sole record of the why behind assembler decisions (Intel-vs-AT&T syntax, NASM local labels, tagged-union operands, hand-coded vs table-driven, function-pointer vs interface fixup resolution) — durable rationale not recoverable from code or git. The bootstrap-subset framing in a few sections is dated but the tradeoff analysis still stands.
- **bootstrap-subset.md** (cat2 [C][t2]) — _status:_ Status note (2026-05-21): the Go bootstrap interpreter has been retired — Its own status note demotes it from source-of-truth, but it remains a detailed, still-useful catalog of which language features exist and their supported-by-X status (operators, precedence, builtins, known spec divergences) referenced from multiple plan docs and CLAUDE.md. Bloated and partly historical, but no single doc replaces the feature catalog.
- **claude-discussion-detailed-notes.md** (cat2 [C][t2]) — _status:_ DECIDED markers per section; doc-level: none — Extended rationale companion to claude-notes.md (alternatives considered and why rejected), named as a key reference in CLAUDE.md and cited by name from claude-notes.md and the interface/vtable plans. Sole durable record of much of the 'why'; not deletable, not bloated enough to slim without losing rationale.
- **claude-website.md** (cat2) — Compact (51 lines) record of website architecture decisions and their rationale (two-repo split, custom SSG over Hugo for eventual self-host port, mistune choice). The 'why' here is not captured in code; small and durable, no bloat to slim.
- **debug-info-status.md** (cat2) — Point-in-time DWARF status (2026-04) but doubles as the de-facto spec of what -g emits and a concrete, still-accurate roadmap of what's missing (per-file DIFile, accurate types, param tagging, scopes) plus code-location pointers. Reads as a living feature-status doc, not a spent plan.
- **design-refcount-axioms.md** (cat2) — The sole written statement of the five refcount axioms (alive->rc>0, rc=0->dtor, copy->copy-ctor, move->zero, assign=copy-then-destroy) that the compiler's refcount machinery is built on and that claude-todo.md entries cite by number ('Axiom 3', 'Axiom 5'). High-value durable design contract; the Option-A/B implementation-plan tail is somewhat spent but the axioms are load-bearing.
- **differences-with-go.md** (cat2) — _status:_ explicitly incomplete (has a TODO backfill section) — An in-progress onboarding reference cataloguing where Binate diverges from Go (memory model, strings, type system, error handling); useful for any Go-fluent reader and explicitly still being backfilled. Not spent execution detail; keep and let it grow.
- **feedback_line_splitting.md** (cat2) — Tiny, durable coding-style preference (prefer brute-force line wrapping over single-use variable extraction) that is still actionable today and not captured elsewhere. Nothing to slim; it's already minimal.
- **ir-backend-guidelines.md** (cat2 [C]) — Durable statement of the IR-vs-backend responsibility split and the compiler/interpreter layout-interop contract; CLAUDE.md cites it as the governing rule for pkg/codegen and pkg/ir work. Still the canonical reference even though several 'currently in codegen, needs move' notes are now stale (the moves landed).
- **notes-package-introspection.md** (cat2 [t2]) — _status:_ Status: design notes, not a plan — Sole record of the unratified RTTI + package-introspection design (unify TypeInfo, function-value interop table, A/B/C phasing) for unimplemented features; claude-todo.md references it as the basis for future RTTI/reflection work. Durable design knowledge worth consulting, not yet superseded.
- **plan-arm32-bare-metal.md** (cat2 [t1]) — _status:_ DRAFT — v0 (arm32-linux) and v1 (arm32-baremetal) have shipped — both conformance modes are live with xfail sets — but v2 (real heap), v3 (real hardware), and v4 (native arm32 backend) remain ahead, and this is the sole detailed record of the boot story, allocator phasing, and bootstrap.bni bare-metal shape driving that work. Slimming the shipped-milestone detail is defensible later, but it is still the reference for in-flight work.
- **plan-bootstrap-ccall.md** (cat2 [t3]) — _status:_ Phase 1 DONE (a7fabc7a); Phase 2 DEFERRED, possibly indefinitely — Phase 1 shipped but Phase 2 is explicitly deferred-not-abandoned, blocked on a BUILDER-runtime-ABI coupling whose non-obvious reasoning (gen1 links BUILDER's pinned runtime → duplicate symbols) and the ready-but-unlandable .bn code are recorded only here. The Stat ABI-divergence and Args argv-hook wrinkles are durable design knowledge for whenever the BUILDER bump happens.
- **plan-const-readonly.md** (cat2 [t2]) — _status:_ COMPLETE (2026-06-03) — Marked COMPLETE but is the sole record of the non-obvious decision to split the overloaded const keyword into const (compile-time scalar) / var / readonly-type-modifier, with the rationale and the deferral list (readonly-slice slicing, &pkg.Const). The why here is not captured by code; keep as the design-of-record (could be slimmed by trimming the bisectable phasing checklist, but content is high-value).
- **plan-debug-hooks.md** (cat2) — _status:_ none (unimplemented — no debugHooks/pre_copy/pre_destroy anywhere in tree) — Not a spent plan: this feature was never started (no references in code or todo lists), so it is a live design proposal for annotation-based pre-copy/pre-destroy hooks, including the resolved call-site-vs-inside-copy-ctor decision. Keep as the sole record of an unbuilt-but-considered feature; not in active use but the design isn't superseded.
- **plan-extern-var.md** (cat2) — _status:_ COMPLETE (2026-06-03) — Marked COMPLETE but documents the non-obvious dotted-name-reuse mechanism (mangle.GlobalName routing via writeBnDotted, ignoring modulePkgName) and the 8-layer landing map plus the VM globals-accumulation fix — design knowledge a maintainer touching cross-package globals needs. Also tracks the still-open managed-ptr value-copy crash (559). Keep; could be slimmed slightly but the per-layer map is the value.
- **plan-interface-syntax-revision.md** (cat2 [t2]) — _status:_ RATIFIED 2026-05-01; IMPLEMENTED 2026-05-23 — Implemented (parser/types/codegen/VM all carry it; todo marks it MOSTLY DONE) but it remains the sole consolidated record of the ratified syntax DECISIONS and their *why* (no *const interface form, explicit-only construction conversions, interface-vs-type distinction) — rationale not captured in code. claude-notes.md still reflects the pre-revision design, so this is the authoritative design source. Mildly bloated with execution checkboxes that could be slimmed, but the decisions justify keeping.
- **plan-linker.md** (cat2) — _status:_ none (Priority: Low, explicitly waiting on stdlib) — Un-started future work (no pkg/link, no cmd/bnld in the tree) and the sole record of the non-obvious 'linker-as-library + Binate-as-linker-script' design decision plus the relocation-type inventory and incremental ordering. Durable forward-looking design, not spent.
- **plan-multi-backend-layout.md** (cat2) — The native backend it sketches now exists (pkg/binate/native with driver + x64/arm32/aarch64 sub-packages, peer to pkg/codegen), so milestones 1-7 are largely realized. But it remains a durable architectural reference: the IR-to-backend boundary table, the shared-substrate vs per-arch ArchSpec split, and the explicit MIR-tier open question — design knowledge worth consulting, complements ir-backend-guidelines.md. Could slim the milestone list once fully done, but the boundary design justifies keeping.
- **plan-phase3-string-unification.md** (cat2) — _status:_ none (DRAFT — multiple unresolved decision points) — Unshipped draft with three open approaches and a tentative recommendation still awaiting a user decision; no LANDED markers and the special string ops it proposes deleting appear to still exist. Records non-obvious rodata-alias/refcount-sentinel design that code does not capture.
- **plan-primitives-impl-interfaces.md** (cat2) — _status:_ Status: LANDED 2026-05-15 (slices 1, A, 2a, 2b); Slice 3 pending — Mostly landed, but this is the sole record of substantive ratified design decisions (Option 2 stdlib carve-out rationale, single-Compare-method interface family, value-receiver iv-dispatch thunks, NaN total ordering) and Slice 3 (println rewrite) is still pending on raw-iface variadics. High durable design value.
- **proposal-restrict-implicit-raw-conversion.md** (cat2) — _status:_ Proposal (needs investigation) — Live, undecided language-design proposal and the sole record of this direction (restrict @T->*T to borrowing positions), including the open questions on syntax, migration impact, and flow analysis. Touches language semantics, which the user owns — keep until decided.

## Caveats / conflicts to resolve before removing

- **CLAUDE.md key-reference list is itself partly stale** — it points at completed/spent plans: `ir-backend-cleanup-plan.md` (→delete), `layout-extraction-plan.md` (→delete), `claude-plan-2.md` (→slim), `plan-function-values.md` (→slim). Prune that list as part of the pass.
- **Anchored deletes need their inbound reference removed too:** `ir-backend-cleanup-plan.md` & `layout-extraction-plan.md` ([C]); `layout-extraction-plan.md`, `plan-const-nonint.md`, `plan-vm-64bit-on-32bit.md`, `plan-vm-compiled-closure-dtor.md` ([t1] — fix/drop the claude-todo.md mention when removing).
- **Verify before deleting `plan-4word-*`** — CLAUDE.md's overview says `@[]T` is 3-word, but these plans (and a saved memory) describe a 4-word managed-slice. Confirm the current representation so we don't delete a still-live design.
