# Completed plans (archive)

Design/implementation plans whose work has fully landed, kept for the design
rationale they record — why decisions were made, which alternatives were
rejected — which stays useful after the work ships.

Active plans live in the parent `explorations/` directory; deferred / post-1.0
ideas live in `../claude-todo-v2.md`; the running done log is `../claude-todo-done.md`.
