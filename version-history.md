# Binate Version History & Build Ladder

The self-hosted compiler (`bnc`) bootstraps from a *builder* — a
previously-built compiler that compiles the current tree.  Each
release is pinned to the builder it was produced with (the repo's
`BUILDER_VERSION` file at the time the release tag was cut).  This
file records that ladder and the salient facts of each release.

`VERSION` = what the current tree will be tagged as next.
`BUILDER_VERSION` = the builder the current tree compiles with.

## Ladder

| Release | Built with (BUILDER) | Tagged | Notes |
|---------|----------------------|--------|-------|
| `bootstrap-0.0.1` | — (Go bootstrap interpreter) | — | The Go-implemented bootstrap interpreter from `github.com/binate/bootstrap`. Not a `bnc` release; the root of the ladder. |
| `bnc-0.0.1` | `bootstrap-0.0.1` | 2026-05-21 | First self-hosted `bnc` release. Compiled by the Go bootstrap. Establishes the release-bundle shape (per-platform tarball: `bin/` + `lib/{pkg,runtime}` + `SHA256SUMS`). |
| `bnc-0.0.2` | `bnc-0.0.1` | 2026-05-25 | First release built by a prior `bnc` (Go bootstrap no longer in the build path). **Promised generics** — generic decls + monomorphization landed after 0.0.1 — **but generics are unusable for many real cases**: the parser rejects `@T` / `@[]T` / `*[]T` type arguments in expression-position generic calls (`f[@T](...)`), so any generic over a managed-pointer / managed-slice / raw-slice element (the common case — e.g. `slices.Append[@ast.Decl]`) can't be written. Bare names and `*T` work. See the CRITICAL entry in `claude-todo.md` ("Generic call type args reject `@T` / `@[]T` / `*[]T`"). Kept in the ladder anyway: 0.0.2 carries other fixes that later commits already build on, so reverting wasn't clean — the parser fix lands on top and `bnc-0.0.3` picks it up. |
| `bnc-0.0.3` | `bnc-0.0.2` | 2026-05-26 | Carries the generic-call type-argument parser fix (binate `18b8047`): `f[@T]` / `f[@[]T]` / `f[*[]T]` now parse, so generics are usable over managed-pointer / managed-slice / raw-slice element types. Validated with the released binary before promoting — `bnc-0.0.3` compiles + runs conformance/492 over all three forms. Promoting it to BUILDER (binate `5a27b65`) is what lets `slices.Append[@T]` be used *inside* cmd/bnc's own (BUILDER-compilable) tree, unblocking the `appendXxxPtr` → `slices.Append[@T]` migration. |
| `bnc-0.0.4` | `bnc-0.0.3` | 2026-05-27 | Carries four substantive threads landed since 0.0.3 (60 commits).  **(1) Phase B full-path symbol mangling** (binate `7f989ad` + follow-ups `f7f8f04` / `2122648` / `4cd596a`): every `/` in a package path folds into the linker symbol (`pkg/asm/x64` → `bn_pkg__asm__x64__…`), so two packages sharing a last-segment name no longer collide and cross-package generic instances emit matching def+call symbols regardless of how slashed the type-arg's full path is — promoting 0.0.4 to BUILDER unblocks the `appendXxxPtr` → `slices.Append[@T]` migration in cmd/bnc's (BUILDER-compilable) tree.  **(2) `__c_call` intrinsic** (`4cd873f` Stage 1a / `ced3b85` Stage 1b / `2a77341` Stage 2 native): direct C-symbol calls with no Binate name mangling and no Binate ABI obligations on the callee, the foundation for the C-free systems target's C-interop layer.  **(3) Host-independent 64-bit integers in the IR + VM** (`035022c` Layer 1 — IR ints stored as int64; Layer 2 register-pair lowering, e.g. `7f112e4` / `7ce4b91` / `c4687f5` / `0a86865` / `944517d`): `int64` arithmetic, calls, multi-returns, casts and memory ops all lower to register-pair bytecode in the VM, removing the host-int assumption that previously gated a 32-bit-hosted toolchain.  **(4) `x86_64-darwin` native target** (`a0bdf62` Mach-O run path, `bda81ca` Backend-interface refactor, `cd42bd6` conformance runner, `f7a182b` / `b719d7e` SysV aggregate-arg passing): third native target after arm64-darwin / arm32-linux, gated by its own conformance runner. |
| `bnc-0.0.5` | `bnc-0.0.4` | 2026-05-31 | **Package-layout split tree** (`done/pkg-layout-plan.md`, shipped here): tier-0 packages move into `ifaces/core/` (interfaces) + `impls/core/{common,libc,baremetal}/` (impls), and the release bundle gains `lib/{ifaces,impls}` alongside `lib/{pkg,runtime}` so build scripts can point per-tier `-I`/`-L` roots at the tarball. `ifaces/stdlib` + `impls/stdlib` ship **empty** — tier-1 deferred to a later effort. ~187 commits. |
| `bnc-0.0.6` | `bnc-0.0.5` | 2026-06-02 | Codegen/VM hardening + new surface (39 commits). Notables: `readonly` type-modifier keyword (`plan-const-readonly` step 2); tier-0 `pkg/builtins/reflect` with the `Package` descriptor type; static-managed-object emitter; native `PlanFrame` reserves outgoing area for all call ops; several codegen/vm file splits. Bundle's `stdlib` dirs still empty. |
| `bnc-0.0.7` | `bnc-0.0.6` | 2026-06-04 | **First release to bundle a populated tier-1 stdlib AND consume it from stage-1** (217 commits). Ships `pkg/std/{errors,strconv,math/big}` + `pkg/stdx/slices` under `ifaces/stdlib` + `impls/stdlib/common`; the dormant stage-1 "BUILDER-first stdlib" `-I`/`-L` reorder (binate `459cc550`) activates once this is the BUILDER, so stage-1 resolves stdlib from the frozen bundle — letting cmd/bnc's tree import full-language stdlib (e.g. `pkg/std/math/big`) without that stdlib being BUILDER-subset-compilable (`done/plan-stdlib-bundle.md`). Also carries the strconv `Parse...` series — exact `ParseFloat` over `math/big`, hex floats both directions — and the managed-aggregate-by-value refcount completeness (ir). **Caveat**: ships a `bni` whose interactive `--repl` segfaults — a pre-existing regression from `b9ca1acc` (ReplSession→interface), E2E `repl.sh` red; REPL is a Tier-1 PoC, not build-critical, so the release was accepted with the fix slated for 0.0.8-pre (see `claude-todo.md`). |
| `bnc-0.0.8` | `bnc-0.0.7` | 2026-06-10 | First real `pkg/std/os` file I/O over libc — minimal `File` + `Open`/`OpenFile`/`Create`, native `open(2)` flags selected at compile time via `build.OS` (325 commits). native/x64 gains the field-per-register multi-return ABI (matching LLVM, not eightbyte coalescing) and routes big multi-return func-values through the retbuf-in-RDI sret convention. Runtime-linking cleanup: `--runtime` is now required to link (auto-discovery / `findRuntime` deleted), and the \"primary root\" concept is gone (baremetal paths resolve via `binate-paths` + `dirOf(--runtime)`). |
| `bnc-0.0.9` | `bnc-0.0.8` | 2026-06-14 | Generics-instantiation hardening (191 commits): bounded instantiation recursion (pre-register placeholder + depth + cycle guards), distinct-type-param non-assignability, and a general `&` addressability gate (one `isAddressable` check, not a whitelist). vm/codegen support >16-byte compiled interface-method returns; strconv + math injected as native-only. native emits managed-struct dtors for native-only packages and the `GlobalInfo` table in the per-package reflect descriptor. |
| `bnc-0.0.10` | `bnc-0.0.9` | 2026-06-23 | Static-data unification (314 commits): impl vtables, func-value vtables + handles, and `_Package` info-node tables all migrate onto `ir.DataGlobal` — one static-data emission path. native ABI fixes: narrow fixed stack args packed at natural size (darwin-aarch64), a func-value's >8th float arg placed on the stack (spill shim), and a float-overflow closure. (The `Release bnc-0.0.10` commit was reverted once and re-cut.) |
| `bnc-0.0.11` | `bnc-0.0.10` | 2026-07-14 | Largest range to date (578 commits). **arm32 soft-float** — compute (const/arith/compare/neg/cast) and across-call-boundary — plus int64/uint64 register-pair support in field / multi-return / sret paths, and generic-impl vtables on arm32 (mirroring x64/aa64). **ffi-export foundation**: `bn_init` made idempotent + the `EmitLibInit` dispatcher (the library-init entry point). A zero-size aggregate now costs 0 argument words, not 1. |
| `bnc-0.0.12` | `bnc-0.0.11` | 2026-08-01 | Strict-align arm32 correctness (235 commits): byte-wise aggregate + closure-capture reads for sub-word data (MMU-less arm32 faults on unaligned words), and a bare-metal empty-argv placeholder so `os.Args()` isn't length 0. Multi-return / interface-dispatch miscompile fixes across all three backends: thunk multi-return value-receiver methods for iface dispatch (silent-miscompile fix), offset-faithful struct/array field coercion, `MultiReturnType` on multi-return externs (x64 cross-pkg sret fix). Named-distinct pointers box into interface values (one raw+managed convention) and do not satisfy their underlying's interfaces. |
| `bnc-0.0.13` | `bnc-0.0.12` | 2026-08-11 | **C runtime deleted** (159 commits): `binate_runtime.c` is gone — the toolchain no longer links any C runtime (a C-free-systems milestone), and the dead C-extern-sret ABI machinery is removed. arm32 VFP float-arg completeness: bank-overflow float args/params routed through func-value + framed-closure shims, plus cyclic VFP param up-shift via a memory-staged parallel move. native arm32 baremetal conformance promoted to **blocking** in CI. |
| `bnc-0.0.14` | `bnc-0.0.13` | 2026-08-25 | native backend breadth (158 commits): `--backend native` now honored for dependencies, not just the main module; codegen + native x64/aa64 **fail loud on an unimplemented IR op** instead of silently dropping it. arm32 gains a frame-pointer chain + `OP_STACK_FRAMES` lowering and variadic float args in GP regs (AAPCS-VFP); `builder-comp_arm32_linux_int` promoted to **blocking** with ILP32 goldens. Self-hosting benchmarks (`perf/self.sh`) added and run in CI. |
| `bnc-0.0.15` | `bnc-0.0.14` | 2026-09-02 | **Native register allocator** (192 commits): a linear-scan register assignment (arch-neutral analysis core — liveness + intervals — in `native/common`) wired into both aa64 and x64 emission with callee-saved homes — the first substantive native codegen-quality step. **bnld interpreted linker drivers**: `bnld -driver` runs a Binate driver that calls the compiled `link` package; `link.bni` shipped via `ifaces/toolchain`; driver search paths taken as `-I`/`--impl-path` flags. bnc gains an aarch64-darwin (macos-arm64) cross-target and plumbs `--link-after-objs` through bnld. `std/os` adds `MkdirTemp`. |
| `bnc-0.0.16` | `bnc-0.0.15` | 2026-09-19 | **SROA (scalar-replacement-of-aggregates) IR optimization** (230 commits): scalar-replace struct / array / managed-struct locals, by-value aggregate returns (`var s = call()`), and call-result SROA over func-value + iface-method calls — the first substantive mid-level IR optimization. **Native register-allocator quality**: spill-cost eviction (evict the cheapest interval, not the newcomer), loop-depth-weighted spill cost, `spansClobber` birth test keyed on DefPos. **native/aarch64 codegen**: arg-bank X0-X7 homing via parallel-move marshalling, single unsigned-compare bounds check, shift-scaled power-of-two array indexing, guard-emitter extraction. **Aggregate-load elision** extended to the raw-pointer `*dst = *src` (S-adjacent) shape plus fusable-materialization elision. **c_export / c_call ABI fixes**: native x64 c_export trampoline marshals SSE-split aggregate params by class (`a9fbb3e5b`); the LLVM c_export facade thunk gains the C-memory / internal-register aggregate quadrant — both the SSE and pure-GP rows (`4579568c8`); `__c_call` with zero trailing varargs no longer miscompiled as non-variadic. Package extractions for `.bni`-surface / file-length hygiene: `irgen`+`irutil` and `iropt` split out of `ir`, runtime value/layout types into `vmdata`. |

## Bumping the builder

Advancing `BUILDER_VERSION` lengthens the build ladder (each link is
a generation that must be reproduced to build from scratch), so only
advance it when there's a substantial language gain to justify the
extra generation — e.g. a feature the tree wants to *use* that the
current builder can't compile.  The cut/promote dance:

1. `VERSION` → `bnc-X.Y.Z` (drop the `-preN` suffix), commit, push, tag
   `bnc-X.Y.Z` → release workflow builds + publishes the tarballs.
2. After the release succeeds, verify `scripts/fetch-builder.sh`
   resolves the new version and a smoke build passes.
3. `BUILDER_VERSION` → `bnc-X.Y.Z`; `VERSION` → `bnc-X.Y.(Z+1)-pre1`;
   commit, push.

(0.0.2 is a cautionary example: it was cut to unblock generics, but
a parser bug left generics largely unusable — a builder bump that
delivered less than its headline feature.  Validate the headline
feature end-to-end *before* cutting, not just that it compiles.)
